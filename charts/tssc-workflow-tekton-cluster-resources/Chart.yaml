apiVersion: v2
name: tssc-workflow-tekton-cluster-resources
description: >-
  A Helm chart for Kubernetes to deploy the cluster level resources required to deploy
  and run TSSC reference workflows using Tekton.

  foo
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0

# Latest version of TSSC that this helm chart is tested with.
appVersion: 0.13.0
